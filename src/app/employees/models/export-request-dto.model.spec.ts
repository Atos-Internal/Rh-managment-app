import { ExportRequestDTO } from './export-request-dto.model';

describe('ExportRequestDTO', () => {
  it('should create an instance', () => {
    expect(new ExportRequestDTO()).toBeTruthy();
  });
});
